#!/bin/sh
# Replace `true` below with your actual test or lint command, e.g.:
#   go test ./...
# When the command fails, ysnp shows the overlay and blocks the push.

if ! true; then
    ysnp &
    exit 1
fi

exit 0
