name: release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: linux-x86_64
          - os: macos-14
            target: macos-arm64
    steps:
      - uses: actions/checkout@v4

      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwayland-dev libwayland-bin \
            libcairo2-dev libjpeg-dev libgif-dev \
            libx11-dev libxrandr-dev libcurl4-openssl-dev \
            wayland-protocols

      - name: Build
        # STATIC_DECODERS only affects the Linux link; the mac Makefile branch
        # ignores it. VERSION is pinned from the tag because the shallow
        # checkout can't always `git describe`. GIPHY_API_KEY is baked in from
        # a repo secret so --giphy works out of the box in release binaries;
        # passed via env (not the command line) to keep it out of process
        # listings, and GitHub masks the secret value in logs.
        env:
          GIPHY_API_KEY: ${{ secrets.GIPHY_API_KEY }}
        run: make STATIC_DECODERS=1 VERSION=${{ github.ref_name }} GIPHY_API_KEY="$GIPHY_API_KEY"

      - name: Test
        run: make test

      - name: Rename binary
        run: mv ysnp ysnp-${{ matrix.target }}

      - uses: actions/upload-artifact@v4
        with:
          name: ysnp-${{ matrix.target }}
          path: ysnp-${{ matrix.target }}
          if-no-files-found: error

  release:
    name: publish release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Generate checksums
        working-directory: dist
        run: |
          chmod +x ysnp-*
          sha256sum ysnp-* > SHA256SUMS

      - name: Publish GitHub release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/ysnp-*
            dist/SHA256SUMS
          generate_release_notes: true
